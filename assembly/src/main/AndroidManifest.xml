<manifest xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools" android:sharedUserId="org.securesystem.insular"
          package="org.securesystem.insular" android:versionCode="45400" android:versionName="4.5.4">

    <application tools:ignore="AllowBackup,GoogleAppIndexingWarning,MissingApplicationIcon">

        <!-- Ensure Run/Debug Configuration in Android Studio to work properly for module-assembly build -->
        <activity android:name="org.securesystem.insular.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
