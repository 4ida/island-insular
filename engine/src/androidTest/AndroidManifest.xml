<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="org.securesystem.insular.permission.FREEZE_PACKAGE" />
    <uses-permission android:name="org.securesystem.insular.permission.LAUNCH_PACKAGE" />
    <application>
        <activity android:name="org.securesystem.insular.api.ApiActivityTest$DummyActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <provider android:authorities="${applicationId}.AIP" android:name="org.securesystem.insular.provisioning.AutoIncrementalProvision" android:enabled="false"/>
    </application>
</manifest>
